<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SangeetApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorOnBackground">@color/MatteBlack</item>
        <item name="colorPrimary">@color/white</item>
        <item name="android:statusBarColor">@color/colorOnBackground</item>
    </style>

    <style name="Theme.SangeetApplication" parent="Base.Theme.SangeetApplication" />

</resources>